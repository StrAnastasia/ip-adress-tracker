import ReactDOM from 'react-dom/client';
import './index.css';
import IPAdressTracker from './feature-ip-tracker/components';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <IPAdressTracker />
);